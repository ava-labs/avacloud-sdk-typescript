/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataSignatureAggregatorGet } from "../../funcs/dataSignatureAggregatorGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAggregatedSignaturesRequest$inboundSchema,
};

export const tool$dataSignatureAggregatorGet: ToolDefinition<typeof args> = {
  name: "data-signature-aggregator-get",
  description: `Get Aggregated Signatures

Get Aggregated Signatures for a P-Chain L1 related Warp Message.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataSignatureAggregatorGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
