/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsLookingGlassGetNftHoldersByContractAddress } from "../../funcs/metricsLookingGlassGetNftHoldersByContractAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetNftHoldersByContractAddressRequest$inboundSchema,
};

export const tool$metricsLookingGlassGetNftHoldersByContractAddress:
  ToolDefinition<typeof args> = {
    name: "metrics-looking-glass_get-nft-holders-by-contract-address",
    description: `Get NFT holders by contract address

Get list of NFT holders and number of NFTs held by contract address.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await metricsLookingGlassGetNftHoldersByContractAddress(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
