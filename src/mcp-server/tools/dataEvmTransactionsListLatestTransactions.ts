/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmTransactionsListLatestTransactions } from "../../funcs/dataEvmTransactionsListLatestTransactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLatestTransactionsRequest$inboundSchema,
};

export const tool$dataEvmTransactionsListLatestTransactions: ToolDefinition<
  typeof args
> = {
  name: "data-evm-transactions_list-latest-transactions",
  description: `List latest transactions

Lists the latest transactions. Filterable by status.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmTransactionsListLatestTransactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
