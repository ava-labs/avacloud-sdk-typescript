/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmTransactionsListLatest } from "../../funcs/dataEvmTransactionsListLatest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLatestTransactionsRequest$inboundSchema,
};

export const tool$dataEvmTransactionsListLatest: ToolDefinition<typeof args> = {
  name: "data-evm-transactions-list-latest",
  description: `List latest transactions

Lists the latest transactions. Filterable by status.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmTransactionsListLatest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
