/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataOperationsPostTransactionExportJob } from "../../funcs/dataOperationsPostTransactionExportJob.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostTransactionExportJobRequestBody$inboundSchema,
};

export const tool$dataOperationsPostTransactionExportJob: ToolDefinition<
  typeof args
> = {
  name: "data-operations_post-transaction-export-job",
  description: `Create transaction export operation

Trigger a transaction export operation with given parameters.

The transaction export operation runs asynchronously in the background. The status of the job can be retrieved from the \`/v1/operations/:operationId\` endpoint using the \`operationId\` returned from this endpoint.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataOperationsPostTransactionExportJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
