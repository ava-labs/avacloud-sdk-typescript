/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataUsageMetricsGetApiLogs } from "../../funcs/dataUsageMetricsGetApiLogs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiLogsRequest$inboundSchema,
};

export const tool$dataUsageMetricsGetApiLogs: ToolDefinition<typeof args> = {
  name: "data-usage-metrics_get-api-logs",
  description: `Get logs for requests made by client

Gets logs for requests made by client over a specified time interval for a specific organization.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataUsageMetricsGetApiLogs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
