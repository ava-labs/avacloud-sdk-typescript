/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmTransactionsListInternalTransactions } from "../../funcs/dataEvmTransactionsListInternalTransactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListInternalTransactionsRequest$inboundSchema,
};

export const tool$dataEvmTransactionsListInternalTransactions: ToolDefinition<
  typeof args
> = {
  name: "data-evm-transactions_list-internal-transactions",
  description: `List internal transactions

Returns a list of internal transactions for an address and chain. Filterable by block range.

Note that the internal transactions list only contains \`CALL\` or \`CALLCODE\` transactions with a non-zero value and \`CREATE\`/\`CREATE2\` transactions. To get a complete list of internal transactions use the \`debug_\` prefixed RPC methods on an archive node.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmTransactionsListInternalTransactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
