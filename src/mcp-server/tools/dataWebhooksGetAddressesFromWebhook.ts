/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataWebhooksGetAddressesFromWebhook } from "../../funcs/dataWebhooksGetAddressesFromWebhook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAddressesFromWebhookRequest$inboundSchema,
};

export const tool$dataWebhooksGetAddressesFromWebhook: ToolDefinition<
  typeof args
> = {
  name: "data-webhooks_get-addresses-from-webhook",
  description: `List adresses by webhook

List adresses by webhook.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataWebhooksGetAddressesFromWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
