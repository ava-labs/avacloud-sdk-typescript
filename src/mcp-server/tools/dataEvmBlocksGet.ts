/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmBlocksGet } from "../../funcs/dataEvmBlocksGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBlockRequest$inboundSchema,
};

export const tool$dataEvmBlocksGet: ToolDefinition<typeof args> = {
  name: "data-evm-blocks-get",
  description: `Get block

Gets the details of an individual block on the EVM-compatible chain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmBlocksGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
