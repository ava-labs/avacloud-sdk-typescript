/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsLookingGlassCompositeQuery } from "../../funcs/metricsLookingGlassCompositeQuery.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CompositeQueryRequestDto$inboundSchema,
};

export const tool$metricsLookingGlassCompositeQuery: ToolDefinition<
  typeof args
> = {
  name: "metrics-looking-glass_composite-query",
  description: `Composite query

Composite query to get list of addresses from multiple subqueries.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsLookingGlassCompositeQuery(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
