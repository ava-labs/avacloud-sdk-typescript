/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataOperationsGetResult } from "../../funcs/dataOperationsGetResult.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOperationResultRequest$inboundSchema,
};

export const tool$dataOperationsGetResult: ToolDefinition<typeof args> = {
  name: "data-operations-get-result",
  description: `Get operation

Gets operation details for the given operation id.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataOperationsGetResult(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
