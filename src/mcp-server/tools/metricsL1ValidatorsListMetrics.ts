/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsL1ValidatorsListMetrics } from "../../funcs/metricsL1ValidatorsListMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTotalL1ValidatorMetricsRequest$inboundSchema,
};

export const tool$metricsL1ValidatorsListMetrics: ToolDefinition<typeof args> =
  {
    name: "metrics-l1-validators-list-metrics",
    description: `Get given metric for all validators

Get given metric's value for all validators.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await metricsL1ValidatorsListMetrics(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
