/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataNftsGetTokenDetails } from "../../funcs/dataNftsGetTokenDetails.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTokenDetailsRequest$inboundSchema,
};

export const tool$dataNftsGetTokenDetails: ToolDefinition<typeof args> = {
  name: "data-nfts_get-token-details",
  description: `Get token details

Gets token details for a specific token of an NFT contract.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataNftsGetTokenDetails(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
