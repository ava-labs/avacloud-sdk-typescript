/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataWebhooksListWebhooks } from "../../funcs/dataWebhooksListWebhooks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListWebhooksRequest$inboundSchema,
};

export const tool$dataWebhooksListWebhooks: ToolDefinition<typeof args> = {
  name: "data-webhooks_list-webhooks",
  description: `List webhooks

Lists webhooks for the user.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataWebhooksListWebhooks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
