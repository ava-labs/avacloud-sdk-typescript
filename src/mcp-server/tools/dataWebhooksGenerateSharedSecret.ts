/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataWebhooksGenerateSharedSecret } from "../../funcs/dataWebhooksGenerateSharedSecret.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dataWebhooksGenerateSharedSecret: ToolDefinition = {
  name: "data-webhooks_generate-shared-secret",
  description: `Generate a shared secret

Generates a new shared secret.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dataWebhooksGenerateSharedSecret(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
