/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataWebhooksUpdateWebhook } from "../../funcs/dataWebhooksUpdateWebhook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateWebhookRequest$inboundSchema,
};

export const tool$dataWebhooksUpdateWebhook: ToolDefinition<typeof args> = {
  name: "data-webhooks_update-webhook",
  description: `Update a webhook

Updates an existing webhook.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataWebhooksUpdateWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
