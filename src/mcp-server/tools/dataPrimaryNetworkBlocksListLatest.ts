/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkBlocksListLatest } from "../../funcs/dataPrimaryNetworkBlocksListLatest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLatestPrimaryNetworkBlocksRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkBlocksListLatest: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-blocks-list-latest",
  description: `List latest blocks

Lists latest blocks on one of the Primary Network chains.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkBlocksListLatest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
