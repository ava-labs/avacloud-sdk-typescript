/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataIcmListByAddress } from "../../funcs/dataIcmListByAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIcmMessagesByAddressRequest$inboundSchema,
};

export const tool$dataIcmListByAddress: ToolDefinition<typeof args> = {
  name: "data-icm-list-by-address",
  description: `List ICM messages by address

Lists ICM messages by address. Ordered by timestamp in descending order.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataIcmListByAddress(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
