/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmAddressBalancesListErc20 } from "../../funcs/dataEvmAddressBalancesListErc20.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListErc20BalancesRequest$inboundSchema,
};

export const tool$dataEvmAddressBalancesListErc20: ToolDefinition<typeof args> =
  {
    name: "data-evm-address-balances-list-erc20",
    description: `List ERC-20 balances

Lists ERC-20 token balances of a wallet address.

Balance at a given block can be retrieved with the \`blockNumber\` parameter.

Balance for specific contracts can be retrieved with the \`contractAddresses\` parameter.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await dataEvmAddressBalancesListErc20(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
