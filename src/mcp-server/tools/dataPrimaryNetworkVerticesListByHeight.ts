/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkVerticesListByHeight } from "../../funcs/dataPrimaryNetworkVerticesListByHeight.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVertexByHeightRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkVerticesListByHeight: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-vertices-list-by-height",
  description: `List vertices by height

Lists vertices at the given vertex height on the X-Chain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkVerticesListByHeight(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
