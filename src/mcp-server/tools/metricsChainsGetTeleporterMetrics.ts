/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsChainsGetTeleporterMetrics } from "../../funcs/metricsChainsGetTeleporterMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeleporterMetricsByChainRequest$inboundSchema,
};

export const tool$metricsChainsGetTeleporterMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics-chains-get-teleporter-metrics",
  description: `Get teleporter metrics for EVM chains

Gets teleporter metrics for an EVM chain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsChainsGetTeleporterMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
