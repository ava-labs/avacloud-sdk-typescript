/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataIcmListIcmMessagesByAddress } from "../../funcs/dataIcmListIcmMessagesByAddress.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIcmMessagesByAddressRequest$inboundSchema,
};

export const tool$dataIcmListIcmMessagesByAddress: ToolDefinition<typeof args> =
  {
    name: "data-icm_list-icm-messages-by-address",
    description: `List ICM messages by address

Lists ICM messages by address. Ordered by timestamp in descending order.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await dataIcmListIcmMessagesByAddress(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
