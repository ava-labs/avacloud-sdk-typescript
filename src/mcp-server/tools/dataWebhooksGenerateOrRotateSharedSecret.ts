/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataWebhooksGenerateOrRotateSharedSecret } from "../../funcs/dataWebhooksGenerateOrRotateSharedSecret.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dataWebhooksGenerateOrRotateSharedSecret: ToolDefinition = {
  name: "data-webhooks_generate-or-rotate-shared-secret",
  description: `Generate or rotate a shared secret

Generates a new shared secret or rotate an existing one.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dataWebhooksGenerateOrRotateSharedSecret(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
