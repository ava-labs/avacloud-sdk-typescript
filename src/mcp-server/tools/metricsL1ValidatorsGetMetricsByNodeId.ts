/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsL1ValidatorsGetMetricsByNodeId } from "../../funcs/metricsL1ValidatorsGetMetricsByNodeId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMetricsByNodeIdRequest$inboundSchema,
};

export const tool$metricsL1ValidatorsGetMetricsByNodeId: ToolDefinition<
  typeof args
> = {
  name: "metrics-l1-validators-get-metrics-by-node-id",
  description: `Get metric values with given node id and timestamp range

Get given metric values for a given node id with or without a timestamp range.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsL1ValidatorsGetMetricsByNodeId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
