/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkTransactionsListActiveStakingTransactions } from "../../funcs/dataPrimaryNetworkTransactionsListActiveStakingTransactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.ListActivePrimaryNetworkStakingTransactionsRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkTransactionsListActiveStakingTransactions:
  ToolDefinition<typeof args> = {
    name: "data-primary-network-transactions-list-active-staking-transactions",
    description: `List staking transactions

Lists active staking transactions on the P-Chain for the supplied addresses.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await dataPrimaryNetworkTransactionsListActiveStakingTransactions(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
