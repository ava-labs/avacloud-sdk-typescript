/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lookingGlassCompositeQuery } from "../../funcs/lookingGlassCompositeQuery.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CompositeQueryRequestDto$inboundSchema,
};

export const tool$lookingGlassCompositeQuery: ToolDefinition<typeof args> = {
  name: "looking-glass-composite-query",
  description: `Composite query

Composite query to get list of addresses from multiple subqueries.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lookingGlassCompositeQuery(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
