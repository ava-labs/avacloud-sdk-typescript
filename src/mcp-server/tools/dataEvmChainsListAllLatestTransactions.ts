/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmChainsListAllLatestTransactions } from "../../funcs/dataEvmChainsListAllLatestTransactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAllLatestTransactionsRequest$inboundSchema,
};

export const tool$dataEvmChainsListAllLatestTransactions: ToolDefinition<
  typeof args
> = {
  name: "data-evm-chains-list-all-latest-transactions",
  description:
    `**[Deprecated]** Lists the latest transactions for all supported EVM chains. Filterable by status.

⚠️ **This operation will be removed in a future release.  Please use /v1/transactions endpoint instead** .`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmChainsListAllLatestTransactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
