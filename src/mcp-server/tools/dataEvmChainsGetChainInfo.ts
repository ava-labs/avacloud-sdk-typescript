/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmChainsGetChainInfo } from "../../funcs/dataEvmChainsGetChainInfo.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetChainInfoRequest$inboundSchema,
};

export const tool$dataEvmChainsGetChainInfo: ToolDefinition<typeof args> = {
  name: "data-evm-chains_get-chain-info",
  description: `Get chain information

Gets chain information for the EVM-compatible chain if supported by AvaCloud.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmChainsGetChainInfo(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
