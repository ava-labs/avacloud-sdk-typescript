/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkTransactionsGet } from "../../funcs/dataPrimaryNetworkTransactionsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTxByHashRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkTransactionsGet: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-transactions-get",
  description: `Get transaction

Gets the details of a single transaction on one of the Primary Network chains.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkTransactionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
