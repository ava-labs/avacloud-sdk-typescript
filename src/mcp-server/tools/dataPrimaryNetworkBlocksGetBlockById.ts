/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkBlocksGetBlockById } from "../../funcs/dataPrimaryNetworkBlocksGetBlockById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBlockByIdRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkBlocksGetBlockById: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-blocks_get-block-by-id",
  description: `Get block

Gets a block by block height or block hash on one of the Primary Network chains.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkBlocksGetBlockById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
