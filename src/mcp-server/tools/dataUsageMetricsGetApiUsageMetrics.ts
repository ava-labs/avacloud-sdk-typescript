/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataUsageMetricsGetApiUsageMetrics } from "../../funcs/dataUsageMetricsGetApiUsageMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiUsageMetricsRequest$inboundSchema,
};

export const tool$dataUsageMetricsGetApiUsageMetrics: ToolDefinition<
  typeof args
> = {
  name: "data-usage-metrics_get-api-usage-metrics",
  description: `Get usage metrics for the Data API

Gets metrics for Data API usage over a specified time interval aggregated at the specified time-duration granularity.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataUsageMetricsGetApiUsageMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
