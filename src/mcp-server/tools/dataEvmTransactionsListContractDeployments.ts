/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmTransactionsListContractDeployments } from "../../funcs/dataEvmTransactionsListContractDeployments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListContractDeploymentsRequest$inboundSchema,
};

export const tool$dataEvmTransactionsListContractDeployments: ToolDefinition<
  typeof args
> = {
  name: "data-evm-transactions_list-contract-deployments",
  description: `List deployed contracts

Lists all contracts deployed by the given address.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmTransactionsListContractDeployments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
