/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkRewardsListHistoricalRewards } from "../../funcs/dataPrimaryNetworkRewardsListHistoricalRewards.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListHistoricalPrimaryNetworkRewardsRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkRewardsListHistoricalRewards:
  ToolDefinition<typeof args> = {
    name: "data-primary-network-rewards-list-historical-rewards",
    description: `List historical rewards

Lists historical rewards on the Primary Network for the supplied addresses.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await dataPrimaryNetworkRewardsListHistoricalRewards(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
