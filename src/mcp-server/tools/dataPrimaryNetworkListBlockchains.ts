/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkListBlockchains } from "../../funcs/dataPrimaryNetworkListBlockchains.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListBlockchainsRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkListBlockchains: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network_list-blockchains",
  description: `List blockchains

Lists all blockchains registered on the network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkListBlockchains(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
