/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkListDelegators } from "../../funcs/dataPrimaryNetworkListDelegators.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListDelegatorsRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkListDelegators: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network_list-delegators",
  description: `List delegators

Lists details for delegators.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkListDelegators(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
