/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmContractsGetDeploymentTransaction } from "../../funcs/dataEvmContractsGetDeploymentTransaction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDeploymentTransactionRequest$inboundSchema,
};

export const tool$dataEvmContractsGetDeploymentTransaction: ToolDefinition<
  typeof args
> = {
  name: "data-evm-contracts-get-deployment-transaction",
  description: `Get deployment transaction

If the address is a smart contract, returns the transaction in which it was deployed.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmContractsGetDeploymentTransaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
