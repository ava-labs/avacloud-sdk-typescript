/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkBalancesListByAddresses } from "../../funcs/dataPrimaryNetworkBalancesListByAddresses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBalancesByAddressesRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkBalancesListByAddresses: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-balances-list-by-addresses",
  description: `Get balances

Gets primary network balances for one of the Primary Network chains for the supplied addresses.

C-Chain balances returned are only the shared atomic memory balance. For EVM balance, use the \`/v1/chains/:chainId/addresses/:addressId/balances:getNative\` endpoint.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkBalancesListByAddresses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
