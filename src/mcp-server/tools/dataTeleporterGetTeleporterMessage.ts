/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataTeleporterGetTeleporterMessage } from "../../funcs/dataTeleporterGetTeleporterMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeleporterMessageRequest$inboundSchema,
};

export const tool$dataTeleporterGetTeleporterMessage: ToolDefinition<
  typeof args
> = {
  name: "data-teleporter_get-teleporter-message",
  description: `**[Deprecated]** Gets a teleporter message by message ID.

⚠️ **This operation will be removed in a future release.  Please use /v1/icm/messages/:messageId endpoint instead** .`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataTeleporterGetTeleporterMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
