/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmChainsSupportedChains } from "../../funcs/dataEvmChainsSupportedChains.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SupportedChainsRequest$inboundSchema,
};

export const tool$dataEvmChainsSupportedChains: ToolDefinition<typeof args> = {
  name: "data-evm-chains_supported-chains",
  description: `List chains

Lists the AvaCloud supported EVM-compatible chains. Filterable by network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmChainsSupportedChains(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
