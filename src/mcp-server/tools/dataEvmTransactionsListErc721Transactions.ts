/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmTransactionsListErc721Transactions } from "../../funcs/dataEvmTransactionsListErc721Transactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListErc721TransactionsRequest$inboundSchema,
};

export const tool$dataEvmTransactionsListErc721Transactions: ToolDefinition<
  typeof args
> = {
  name: "data-evm-transactions_list-erc721-transactions",
  description: `List ERC-721 transfers

Lists ERC-721 transfers for an address. Filterable by block range.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmTransactionsListErc721Transactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
