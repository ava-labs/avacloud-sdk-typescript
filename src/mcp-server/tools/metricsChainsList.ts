/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsChainsList } from "../../funcs/metricsChainsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListChainsRequest$inboundSchema,
};

export const tool$metricsChainsList: ToolDefinition<typeof args> = {
  name: "metrics-chains-list",
  description: `Get a list of supported blockchains

Get a list of Metrics API supported blockchains.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsChainsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
