/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsChainMetricsGetEvmChainMetrics } from "../../funcs/metricsChainMetricsGetEvmChainMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetEvmChainMetricsRequest$inboundSchema,
};

export const tool$metricsChainMetricsGetEvmChainMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics-chain-metrics_get-evm-chain-metrics",
  description: `Get metrics for EVM chains

Gets metrics for an EVM chain over a specified time interval aggregated at the specified time-interval granularity.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsChainMetricsGetEvmChainMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
