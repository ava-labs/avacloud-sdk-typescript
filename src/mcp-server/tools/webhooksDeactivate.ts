/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksDeactivate } from "../../funcs/webhooksDeactivate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeactivateWebhookRequest$inboundSchema,
};

export const tool$webhooksDeactivate: ToolDefinition<typeof args> = {
  name: "webhooks-deactivate",
  description: `Deactivate a webhook

Deactivates a webhook by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksDeactivate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
