/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataSignatureAggregatorAggregate } from "../../funcs/dataSignatureAggregatorAggregate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AggregateSignaturesRequest$inboundSchema,
};

export const tool$dataSignatureAggregatorAggregate: ToolDefinition<
  typeof args
> = {
  name: "data-signature-aggregator-aggregate",
  description: `Aggregate Signatures

Aggregates Signatures for a Warp message from Subnet validators.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataSignatureAggregatorAggregate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
