/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmAddressTransactionsListNative } from "../../funcs/dataEvmAddressTransactionsListNative.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListNativeTransactionsRequest$inboundSchema,
};

export const tool$dataEvmAddressTransactionsListNative: ToolDefinition<
  typeof args
> = {
  name: "data-evm-address-transactions-list-native",
  description: `List native transactions

Lists native transactions for an address. Filterable by block range.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmAddressTransactionsListNative(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
