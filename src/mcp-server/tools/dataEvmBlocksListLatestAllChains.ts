/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmBlocksListLatestAllChains } from "../../funcs/dataEvmBlocksListLatestAllChains.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLatestBlocksAllChainsRequest$inboundSchema,
};

export const tool$dataEvmBlocksListLatestAllChains: ToolDefinition<
  typeof args
> = {
  name: "data-evm-blocks-list-latest-all-chains",
  description: `List latest blocks across all supported EVM chains

Lists the most recent blocks from all supported  EVM-compatible chains. The results can be filtered by network.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmBlocksListLatestAllChains(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
