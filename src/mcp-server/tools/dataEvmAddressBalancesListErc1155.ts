/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmAddressBalancesListErc1155 } from "../../funcs/dataEvmAddressBalancesListErc1155.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListErc1155BalancesRequest$inboundSchema,
};

export const tool$dataEvmAddressBalancesListErc1155: ToolDefinition<
  typeof args
> = {
  name: "data-evm-address-balances-list-erc1155",
  description: `List ERC-1155 balances

Lists ERC-1155 token balances of a wallet address.

Balance at a given block can be retrieved with the \`blockNumber\` parameter.

Balance for a specific contract can be retrieved with the \`contractAddress\` parameter.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmAddressBalancesListErc1155(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
