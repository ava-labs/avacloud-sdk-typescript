/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmChainsListAddressChains } from "../../funcs/dataEvmChainsListAddressChains.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAddressChainsRequest$inboundSchema,
};

export const tool$dataEvmChainsListAddressChains: ToolDefinition<typeof args> =
  {
    name: "data-evm-chains_list-address-chains",
    description: `List all chains associated with a given address

Lists the chains where the specified address has  participated in transactions or ERC token transfers,  either as a sender or receiver. The data is refreshed every 15  minutes.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await dataEvmChainsListAddressChains(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
