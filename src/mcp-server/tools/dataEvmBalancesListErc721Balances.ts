/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmBalancesListErc721Balances } from "../../funcs/dataEvmBalancesListErc721Balances.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListErc721BalancesRequest$inboundSchema,
};

export const tool$dataEvmBalancesListErc721Balances: ToolDefinition<
  typeof args
> = {
  name: "data-evm-balances_list-erc721-balances",
  description: `List ERC-721 balances

Lists ERC-721 token balances of a wallet address.

Balance for a specific contract can be retrieved with the \`contractAddress\` parameter.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmBalancesListErc721Balances(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
