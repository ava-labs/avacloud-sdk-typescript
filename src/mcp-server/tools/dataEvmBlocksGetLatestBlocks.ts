/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmBlocksGetLatestBlocks } from "../../funcs/dataEvmBlocksGetLatestBlocks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLatestBlocksRequest$inboundSchema,
};

export const tool$dataEvmBlocksGetLatestBlocks: ToolDefinition<typeof args> = {
  name: "data-evm-blocks_get-latest-blocks",
  description: `List latest blocks

Lists the latest indexed blocks on the EVM-compatible chain sorted in descending order by block timestamp.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmBlocksGetLatestBlocks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
