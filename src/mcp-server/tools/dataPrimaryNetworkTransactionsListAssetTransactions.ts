/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkTransactionsListAssetTransactions } from "../../funcs/dataPrimaryNetworkTransactionsListAssetTransactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAssetTransactionsRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkTransactionsListAssetTransactions:
  ToolDefinition<typeof args> = {
    name: "data-primary-network-transactions_list-asset-transactions",
    description: `List asset transactions

Lists asset transactions corresponding to the given asset id on the X-Chain.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await dataPrimaryNetworkTransactionsListAssetTransactions(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
