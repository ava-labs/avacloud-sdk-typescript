/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkUtxosGetUtxosByAddresses } from "../../funcs/dataPrimaryNetworkUtxosGetUtxosByAddresses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUtxosByAddressesRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkUtxosGetUtxosByAddresses: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-utxos_get-utxos-by-addresses",
  description: `List UTXOs

Lists UTXOs on one of the Primary Network chains for the supplied addresses.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkUtxosGetUtxosByAddresses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
