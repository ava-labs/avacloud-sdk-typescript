/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataUsageMetricsGetSubnetRpcUsageMetrics } from "../../funcs/dataUsageMetricsGetSubnetRpcUsageMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSubnetRpcUsageMetricsRequest$inboundSchema,
};

export const tool$dataUsageMetricsGetSubnetRpcUsageMetrics: ToolDefinition<
  typeof args
> = {
  name: "data-usage-metrics_get-subnet-rpc-usage-metrics",
  description: `Get usage metrics for the Subnet RPC

Gets metrics for public Subnet RPC usage over a specified time interval aggregated at the specified time-duration granularity.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataUsageMetricsGetSubnetRpcUsageMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
