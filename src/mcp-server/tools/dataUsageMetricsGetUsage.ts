/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataUsageMetricsGetUsage } from "../../funcs/dataUsageMetricsGetUsage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiUsageMetricsRequest$inboundSchema,
};

export const tool$dataUsageMetricsGetUsage: ToolDefinition<typeof args> = {
  name: "data-usage-metrics-get-usage",
  description: `Get usage metrics for the Data API

Gets metrics for Data API usage over a specified time interval aggregated at the specified time-duration granularity.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataUsageMetricsGetUsage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
