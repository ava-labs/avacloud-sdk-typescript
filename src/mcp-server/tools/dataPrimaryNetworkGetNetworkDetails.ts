/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkGetNetworkDetails } from "../../funcs/dataPrimaryNetworkGetNetworkDetails.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetNetworkDetailsRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkGetNetworkDetails: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network_get-network-details",
  description: `Get network details

Gets network details such as validator and delegator stats.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkGetNetworkDetails(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
