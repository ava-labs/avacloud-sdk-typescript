/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmAddressContractsListDeployments } from "../../funcs/dataEvmAddressContractsListDeployments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListContractDeploymentsRequest$inboundSchema,
};

export const tool$dataEvmAddressContractsListDeployments: ToolDefinition<
  typeof args
> = {
  name: "data-evm-address-contracts-list-deployments",
  description: `List deployed contracts

Lists all contracts deployed by the given address.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmAddressContractsListDeployments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
