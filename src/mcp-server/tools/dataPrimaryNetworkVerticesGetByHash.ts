/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkVerticesGetByHash } from "../../funcs/dataPrimaryNetworkVerticesGetByHash.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetVertexByHashRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkVerticesGetByHash: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-vertices-get-by-hash",
  description: `Get vertex

Gets a single vertex on the X-Chain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkVerticesGetByHash(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
