/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkGetAssetDetails } from "../../funcs/dataPrimaryNetworkGetAssetDetails.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAssetDetailsRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkGetAssetDetails: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network_get-asset-details",
  description: `Get asset details

Gets asset details corresponding to the given asset id on the X-Chain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkGetAssetDetails(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
