/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataWebhooksGetWebhook } from "../../funcs/dataWebhooksGetWebhook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWebhookRequest$inboundSchema,
};

export const tool$dataWebhooksGetWebhook: ToolDefinition<typeof args> = {
  name: "data-webhooks_get-webhook",
  description: `Get a webhook by ID

Retrieves a webhook by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataWebhooksGetWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
