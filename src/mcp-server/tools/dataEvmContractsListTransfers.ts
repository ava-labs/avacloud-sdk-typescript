/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmContractsListTransfers } from "../../funcs/dataEvmContractsListTransfers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTransfersRequest$inboundSchema,
};

export const tool$dataEvmContractsListTransfers: ToolDefinition<typeof args> = {
  name: "data-evm-contracts-list-transfers",
  description: `List ERC transfers

Lists ERC transfers for an ERC-20, ERC-721, or ERC-1155 contract address.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmContractsListTransfers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
