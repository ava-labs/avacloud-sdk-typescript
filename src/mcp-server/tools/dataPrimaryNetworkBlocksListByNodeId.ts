/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkBlocksListByNodeId } from "../../funcs/dataPrimaryNetworkBlocksListByNodeId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPrimaryNetworkBlocksByNodeIdRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkBlocksListByNodeId: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-blocks-list-by-node-id",
  description: `List blocks proposed by node

Lists the latest blocks proposed by a given NodeID on one of the Primary Network chains.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkBlocksListByNodeId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
