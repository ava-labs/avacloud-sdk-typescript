/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataWebhooksDeactivateWebhook } from "../../funcs/dataWebhooksDeactivateWebhook.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeactivateWebhookRequest$inboundSchema,
};

export const tool$dataWebhooksDeactivateWebhook: ToolDefinition<typeof args> = {
  name: "data-webhooks_deactivate-webhook",
  description: `Deactivate a webhook

Deactivates a webhook by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataWebhooksDeactivateWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
