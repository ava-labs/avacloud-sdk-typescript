/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksAddressesRemove } from "../../funcs/webhooksAddressesRemove.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAddressesFromWebhookRequest$inboundSchema,
};

export const tool$webhooksAddressesRemove: ToolDefinition<typeof args> = {
  name: "webhooks-addresses-remove",
  description: `Remove addresses from EVM activity  webhook

Remove addresses from webhook. Only valid for EVM activity webhooks.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksAddressesRemove(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
