/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataIcmList } from "../../funcs/dataIcmList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListIcmMessagesRequest$inboundSchema,
};

export const tool$dataIcmList: ToolDefinition<typeof args> = {
  name: "data-icm-list",
  description: `List ICM messages

Lists ICM messages. Ordered by timestamp in descending order.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataIcmList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
