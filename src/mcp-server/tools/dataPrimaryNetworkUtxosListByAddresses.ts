/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkUtxosListByAddresses } from "../../funcs/dataPrimaryNetworkUtxosListByAddresses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUtxosByAddressesRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkUtxosListByAddresses: ToolDefinition<
  typeof args
> = {
  name: "data-primary-network-utxos-list-by-addresses",
  description: `List UTXOs

Lists UTXOs on one of the Primary Network chains for the supplied addresses.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataPrimaryNetworkUtxosListByAddresses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
