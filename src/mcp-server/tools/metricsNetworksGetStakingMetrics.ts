/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsNetworksGetStakingMetrics } from "../../funcs/metricsNetworksGetStakingMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStakingMetricsRequest$inboundSchema,
};

export const tool$metricsNetworksGetStakingMetrics: ToolDefinition<
  typeof args
> = {
  name: "metrics-networks-get-staking-metrics",
  description: `Get staking metrics for a given subnet

Gets staking metrics for a given subnet.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsNetworksGetStakingMetrics(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
