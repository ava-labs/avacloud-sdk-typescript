/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataNftsReindexNft } from "../../funcs/dataNftsReindexNft.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ReindexNftRequest$inboundSchema,
};

export const tool$dataNftsReindexNft: ToolDefinition<typeof args> = {
  name: "data-nfts_reindex-nft",
  description: `Reindex NFT metadata

Triggers reindexing of token metadata for an NFT token. Reindexing can only be called once per hour for each NFT token.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataNftsReindexNft(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
