/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsChainsListTokenHoldersAboveThreshold } from "../../funcs/metricsChainsListTokenHoldersAboveThreshold.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAddressesByBalanceOverTimeRequest$inboundSchema,
};

export const tool$metricsChainsListTokenHoldersAboveThreshold: ToolDefinition<
  typeof args
> = {
  name: "metrics-chains-list-token-holders-above-threshold",
  description: `Get addresses by balance over time

Get list of addresses and their latest balances that have held more than a certain threshold of a given token during the specified time frame.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsChainsListTokenHoldersAboveThreshold(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
