/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkGetSubnetById } from "../../funcs/dataPrimaryNetworkGetSubnetById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSubnetByIdRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkGetSubnetById: ToolDefinition<typeof args> =
  {
    name: "data-primary-network_get-subnet-by-id",
    description: `Get Subnet details by ID

Get details of the Subnet registered on the network.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await dataPrimaryNetworkGetSubnetById(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
