/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataPrimaryNetworkVerticesListLatestXChainVertices } from "../../funcs/dataPrimaryNetworkVerticesListLatestXChainVertices.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListLatestXChainVerticesRequest$inboundSchema,
};

export const tool$dataPrimaryNetworkVerticesListLatestXChainVertices:
  ToolDefinition<typeof args> = {
    name: "data-primary-network-vertices_list-latest-X-chain-vertices",
    description: `List vertices

Lists latest vertices on the X-Chain.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await dataPrimaryNetworkVerticesListLatestXChainVertices(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
