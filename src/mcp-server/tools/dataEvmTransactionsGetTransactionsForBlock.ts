/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmTransactionsGetTransactionsForBlock } from "../../funcs/dataEvmTransactionsGetTransactionsForBlock.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTransactionsForBlockRequest$inboundSchema,
};

export const tool$dataEvmTransactionsGetTransactionsForBlock: ToolDefinition<
  typeof args
> = {
  name: "data-evm-transactions_get-transactions-for-block",
  description: `List transactions for a block

Lists the transactions that occured in a given block.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmTransactionsGetTransactionsForBlock(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
