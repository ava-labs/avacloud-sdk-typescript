/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataEvmBalancesListCollectibleBalances } from "../../funcs/dataEvmBalancesListCollectibleBalances.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListCollectibleBalancesRequest$inboundSchema,
};

export const tool$dataEvmBalancesListCollectibleBalances: ToolDefinition<
  typeof args
> = {
  name: "data-evm-balances_list-collectible-balances",
  description: `List collectible (ERC-721/ERC-1155) balances

Lists ERC-721 and ERC-1155 token balances of a wallet address.

Balance for a specific contract can be retrieved with the \`contractAddress\` parameter.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataEvmBalancesListCollectibleBalances(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
